import { ExtensionList } from './extension-list';

/**
 * This file works much like the standard app.routes.ts, it gets included and provides an array to the
 * extension manager to inform it which extensions to load.
 */

export const extensions: ExtensionList = [
/*    {
        name: "Extension Name",
        class: ExtensionClassName
    }*/
];